info:
  name: Add Book
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/books
  body:
    type: json
    data: |-
      {
        "id": 22,
        "title": "Programming TypeScript in english",
        "author": "Boris Cherny"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
